/*accept any two numbers and display eaual or uneaual number*/
#include <stdio.h>
int main()
{int a,b,equal;
     printf("enter any two numbers ");
     scanf("%d%d",&a,&b);
	    
		
     if(a==b)
	 printf("both numbers are equal");
	 else
	 printf("both numbers are unequal");    
}
